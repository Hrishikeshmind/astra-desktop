/**
 * Retry utilities for safeRm/safeRmSync with exponential backoff.
 */
/**
 * Default options for safe variants.
 * Windows gets automatic retries, POSIX does not.
 */
export declare const SAFE_DEFAULTS: {
    recursive: boolean;
    force: boolean;
    maxRetries: number;
    retryDelay: number;
};
/**
 * Check if an error is retryable.
 */
export declare function isRetryableError(err: unknown): boolean;
/**
 * Calculate exponential backoff delay.
 * delay * (factor ^ attempt)
 */
export declare function getBackoffDelay(baseDelay: number, attempt: number): number;
/**
 * Busy-wait for sync operations.
 */
export declare function busyWait(ms: number): void;
