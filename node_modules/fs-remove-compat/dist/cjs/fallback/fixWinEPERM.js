"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get fixWinEPERM () {
        return fixWinEPERM;
    },
    get fixWinEPERMSync () {
        return fixWinEPERMSync;
    },
    get shouldFixEPERM () {
        return shouldFixEPERM;
    }
});
var _fs = /*#__PURE__*/ _interop_require_default(require("fs"));
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
var isWindows = process.platform === 'win32' || /^(msys|cygwin)$/.test(process.env.OSTYPE);
function fixWinEPERM(path, originalError, callback) {
    _fs.default.chmod(path, 438, function(chmodErr) {
        if (chmodErr) {
            // chmod failed, return original error
            callback(originalError);
            return;
        }
        _fs.default.stat(path, function(statErr, stats) {
            if (statErr) {
                // stat failed, return original error
                callback(originalError);
                return;
            }
            if (stats.isDirectory()) {
                _fs.default.rmdir(path, callback);
            } else {
                _fs.default.unlink(path, callback);
            }
        });
    });
}
function fixWinEPERMSync(path, originalError) {
    try {
        _fs.default.chmodSync(path, 438);
    } catch (_chmodErr) {
        throw originalError;
    }
    var stats;
    try {
        stats = _fs.default.statSync(path);
    } catch (_statErr) {
        throw originalError;
    }
    if (stats.isDirectory()) {
        _fs.default.rmdirSync(path);
    } else {
        _fs.default.unlinkSync(path);
    }
}
function shouldFixEPERM(err) {
    return isWindows && err.code === 'EPERM';
}
/* CJS INTEROP */ if (exports.__esModule && exports.default) { try { Object.defineProperty(exports.default, '__esModule', { value: true }); for (var key in exports) { exports.default[key] = exports[key]; } } catch (_) {}; module.exports = exports.default; }