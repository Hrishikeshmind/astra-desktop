"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "default", {
    enumerable: true,
    get: function() {
        return makeRequest;
    }
});
var _http = /*#__PURE__*/ _interop_require_wildcard(require("http"));
var _https = /*#__PURE__*/ _interop_require_wildcard(require("https"));
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
var URL_REGEX = /^(([^:/?#]+):)?(\/\/([^/?#]*))?([^?#]*)(\?([^#]*))?(#(.*))?/;
function makeRequest(endpoint, optionsOrCallback, callback) {
    var options = typeof optionsOrCallback === 'function' ? {} : optionsOrCallback;
    var cb = typeof optionsOrCallback === 'function' ? optionsOrCallback : callback;
    // url.parse replacement
    var parsed = URL_REGEX.exec(endpoint);
    var protocol = parsed[1];
    var host = parsed[4];
    var path = parsed[5] + (parsed[6] || '');
    var secure = protocol === 'https:';
    var method = options.method || 'GET';
    var requestOptions = {
        host: host,
        path: path,
        port: secure ? 443 : 80,
        method: method
    };
    var req = secure ? _https.request(requestOptions) : _http.request(requestOptions);
    var called = false;
    var end = function(err, res) {
        if (called) return;
        called = true;
        cb(err, res);
    };
    if (options.timeout) {
        req.setTimeout(options.timeout, function() {
            req.abort();
            end(new Error("Request timeout after ".concat(options.timeout, "ms")));
        });
    }
    req.on('response', function(res) {
        // Follow 3xx redirects
        if (res.statusCode >= 300 && res.statusCode < 400 && res.headers.location) {
            res.resume(); // Discard response
            return makeRequest(res.headers.location, options, cb);
        }
        // Not successful
        if (res.statusCode < 200 || res.statusCode >= 300) {
            res.resume(); // Discard response
            return end(new Error("Response code ".concat(res.statusCode, " (").concat(_http.STATUS_CODES[res.statusCode], ")")));
        }
        end(null, res);
    });
    req.on('error', end);
    req.end();
}
/* CJS INTEROP */ if (exports.__esModule && exports.default) { try { Object.defineProperty(exports.default, '__esModule', { value: true }); for (var key in exports) { exports.default[key] = exports[key]; } } catch (_) {}; module.exports = exports.default; }