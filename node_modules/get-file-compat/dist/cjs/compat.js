"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get bufferFrom () {
        return bufferFrom;
    },
    get readableFrom () {
        return readableFrom;
    }
});
var _stream = /*#__PURE__*/ _interop_require_default(require("stream"));
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function bufferFrom(data, encoding) {
    if (typeof Buffer.from === 'function') {
        try {
            return Buffer.from(data, encoding);
        } catch (_err) {
        // Buffer.from(string) throws in Node 4.x for some cases, fall through
        }
    }
    // eslint-disable-next-line no-buffer-constructor
    return new Buffer(data, encoding);
}
function readableFrom(buffer) {
    var ReadableClass = _stream.default.Readable;
    var PassThroughClass = _stream.default.PassThrough;
    // Try Readable.from if available (Node 12.3.0+)
    if (ReadableClass && typeof ReadableClass.from === 'function') return ReadableClass.from(buffer);
    // Use PassThrough if available (Node 0.10+)
    if (PassThroughClass) {
        var pt = new PassThroughClass();
        // Use nextTick to ensure listeners can be attached before data is emitted
        process.nextTick(function() {
            return pt.end(buffer);
        });
        return pt;
    }
    // Fallback for Node 0.10.x where PassThrough might not exist but Readable does
    if (ReadableClass) {
        var readable = new ReadableClass();
        readable._read = function() {};
        // Use nextTick to ensure listeners can be attached before data is emitted
        process.nextTick(function() {
            readable.push(buffer);
            readable.push(null);
        });
        return readable;
    }
    // Node < 0.10 - stream classes not available
    throw new Error('getStream requires Node 0.10 or higher. Use getContent instead.');
}
/* CJS INTEROP */ if (exports.__esModule && exports.default) { try { Object.defineProperty(exports.default, '__esModule', { value: true }); for (var key in exports) { exports.default[key] = exports[key]; } } catch (_) {}; module.exports = exports.default; }