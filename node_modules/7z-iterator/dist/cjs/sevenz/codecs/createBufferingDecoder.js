// Helper to create a Transform stream that buffers all input before decoding
// Used by codecs that need the full input before decompression (LZMA, LZMA2, BZip2, etc.)
"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, /**
 * Create a Transform stream that buffers all input, then decodes in flush
 * This is the common pattern for codecs that can't stream (need full input)
 */ "default", {
    enumerable: true,
    get: function() {
        return createBufferingDecoder;
    }
});
var _extractbaseiterator = require("extract-base-iterator");
function createBufferingDecoder(decodeFn, properties, unpackSize) {
    var chunks = [];
    return new _extractbaseiterator.Transform({
        transform: function(chunk, _encoding, callback) {
            chunks.push(chunk);
            callback();
        },
        flush: function flush(callback) {
            var _this = this;
            var input = Buffer.concat(chunks);
            var finish = function(err, output) {
                if (err) {
                    callback(err);
                    return;
                }
                if (output) {
                    _this.push(output);
                }
                callback();
            };
            try {
                var maybeResult = decodeFn(input, properties, unpackSize, finish);
                if (maybeResult && typeof maybeResult.then === 'function') {
                    maybeResult.then(function(value) {
                        return finish(null, value);
                    }, function(err) {
                        return finish(err);
                    });
                    return;
                }
                if (Buffer.isBuffer(maybeResult)) {
                    finish(null, maybeResult);
                }
            } catch (err) {
                callback(err);
            }
        }
    });
}
/* CJS INTEROP */ if (exports.__esModule && exports.default) { try { Object.defineProperty(exports.default, '__esModule', { value: true }); for (var key in exports) { exports.default[key] = exports[key]; } } catch (_) {}; module.exports = exports.default; }