'use strict'
Object.defineProperty(exports, '__esModule', { value: true })
exports.internalMozconfg = void 0
const __1 = require('..')
const package_1 = require('../commands/package')
const utils_1 = require('../utils')
const otherBuildModes = `# You can change to other build modes by running:
#   $ surfer set buildMode [dev|debug|release]`
const internalMozconfg = (brand, buildMode) => {
  let buildOptions = `# Unknown build mode ${buildMode}`
  // Get the specific build options for the current build mode
  switch (buildMode) {
    case 'dev': {
      buildOptions = `# Development build settings
${otherBuildModes}
ac_add_options --disable-debug`
      break
    }
    case 'debug': {
      buildOptions = `# Debug build settings
${otherBuildModes}
ac_add_options --enable-debug
ac_add_options --disable-optimize`
      break
    }
    case 'release': {
      buildOptions = `# Release build settings
ac_add_options --disable-debug
ac_add_options --enable-optimize
ac_add_options --enable-rust-simd`
      break
    }
  }
  return (
    `
# =====================
# Internal surfer config
# =====================

${buildOptions}

# Custom branding
ac_add_options --with-branding=browser/branding/${brand}

# Config for updates
ac_add_options --enable-unverified-updates
ac_add_options --enable-update-channel=${brand}

export ZEN_FIREFOX_VERSION=${(0, utils_1.getFFVersionOrCandidate)()}
export MOZ_APPUPDATE_HOST=${
      __1.config.updateHostname || 'localhost:7648 # This should not resolve'
    }
` +
    (process.surferPlatform === 'darwin'
      ? `

# MacOS specific settings
export MOZ_MACBUNDLE_NAME="${(0, package_1.getCurrentBrandName)()}.app"
  `
      : '')
  )
}
exports.internalMozconfg = internalMozconfg
