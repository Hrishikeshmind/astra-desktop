"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateAddonUpdateFiles = void 0;
const promises_1 = require("node:fs/promises");
const node_path_1 = require("node:path");
const xmlbuilder2_1 = require("xmlbuilder2");
const constants_1 = require("../../constants");
const utils_1 = require("../../utils");
const addon_1 = require("../download/addon");
async function generateAddonUpdateFiles() {
    const addons = [];
    for (const addon of (0, addon_1.getAddons)()) {
        const url = await (0, addon_1.resolveAddonDownloadUrl)(addon);
        const xpi = await (0, addon_1.downloadAddon)(url, addon);
        addons.push({
            ...addon,
            url,
            xpi,
            hash: await (0, utils_1.generateHash)(xpi, 'sha256'),
            hashType: 'sha256',
            size: await (0, utils_1.getSize)(xpi),
        });
    }
    const root = (0, xmlbuilder2_1.create)().ele('updates').ele('addons');
    for (const addon of addons) {
        const addonNode = root.ele('addon');
        addonNode.att('id', addon.id);
        addonNode.att('URL', addon.url);
        addonNode.att('hashFunction', addon.hashType);
        addonNode.att('hashValue', addon.hash);
        addonNode.att('size', addon.size.toString());
        addonNode.att('version', addon.version);
    }
    const path = (0, node_path_1.join)(constants_1.DIST_DIR, 'update/browser/addons', utils_1.dynamicConfig.get('brand'), 'update.xml');
    await (0, utils_1.ensureDirectory)((0, node_path_1.dirname)(path));
    await (0, promises_1.writeFile)(path, root.end({ prettyPrint: true }));
}
exports.generateAddonUpdateFiles = generateAddonUpdateFiles;
