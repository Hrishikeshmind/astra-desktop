'use strict'
Object.defineProperty(exports, '__esModule', { value: true })
exports.set = void 0
const __1 = require('..')
const log_1 = require('../log')
const utils_1 = require('../utils')
const set = (key, value) => {
  if (key == 'version') {
    console.log(
      __1.config.brands[utils_1.dynamicConfig.get('brand')].release
        .displayVersion
    )
    return
  }
  if (!(key in utils_1.dynamicConfig.defaultValues)) {
    log_1.log.warning(
      `The key ${key} is not found within the dynamic config options`
    )
    return
  }
  if (value) {
    utils_1.dynamicConfig.set(key, value)
    log_1.log.info(`Set ${key} to ${value}`)
    return
  }
  console.log(utils_1.dynamicConfig.get(key))
}
exports.set = set
