"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.init = void 0;
const node_fs_1 = require("node:fs");
const node_path_1 = require("node:path");
const __1 = require("..");
const log_1 = require("../log");
const utils_1 = require("../utils");
const init = async (directory) => {
    const cwd = process.cwd();
    const absoluteInitDirectory = (0, node_path_1.resolve)(cwd, directory.toString());
    if (!(0, node_fs_1.existsSync)(absoluteInitDirectory)) {
        log_1.log.error(`Directory "${directory}" not found.\nCheck the directory exists and run |${__1.bin_name} init| again.`);
    }
    let version = (0, node_fs_1.readFileSync)((0, node_path_1.resolve)(cwd, directory.toString(), 'browser', 'config', 'version_display.txt')).toString();
    if (!version)
        log_1.log.error(`Directory "${directory}" not found.\nCheck the directory exists and run |${__1.bin_name} init| again.`);
    version = version.trim().replace(/\\n/g, '');
    if (process.env.ZEN_DOWNLOAD_DONT_INIT_GIT === '1') {
        log_1.log.info('Skipping git initialization as ZEN_DOWNLOAD_DONT_INIT_GIT is set to 1');
        return;
    }
    // TODO: Use bash on windows, this may significantly improve performance.
    // Still needs testing though
    log_1.log.info('Initializing git, this may take some time');
    await (0, utils_1.configDispatch)('git', {
        args: ['init'],
        cwd: absoluteInitDirectory,
    });
    await (0, utils_1.configDispatch)('git', {
        args: ['checkout', '--orphan', version],
        cwd: absoluteInitDirectory,
    });
    await (0, utils_1.configDispatch)('git', {
        args: ['add', '-f', '.'],
        cwd: absoluteInitDirectory,
    });
    await (0, utils_1.configDispatch)('git', {
        args: ['config', 'commit.gpgsign', 'false'],
        cwd: absoluteInitDirectory,
    });
    await (0, utils_1.configDispatch)('git', {
        args: ['config', 'core.safecrlf', 'false'],
        cwd: absoluteInitDirectory,
    });
    log_1.log.info('Committing...');
    await (0, utils_1.configDispatch)('git', {
        args: ['commit', '-aqm', `"Firefox ${version}"`],
        cwd: absoluteInitDirectory,
        // Committing can fail for configuration issues: see https://github.com/zen-browser/desktop/issues/1877
        killOnError: true,
    });
    await (0, utils_1.configDispatch)('git', {
        args: ['checkout', '-b', utils_1.config.name.toLowerCase().replace(/\s/g, '_')],
        cwd: absoluteInitDirectory,
    });
};
exports.init = init;
