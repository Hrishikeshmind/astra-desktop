import { safeRmSync } from 'fs-remove-compat';
import { DEFAULT_CACHE_PATH } from '../constants.js';
export default function clear(_options) {
    safeRmSync(DEFAULT_CACHE_PATH);
    console.log(`Cleaned ${DEFAULT_CACHE_PATH}`);
}
