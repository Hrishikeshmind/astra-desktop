"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "default", {
    enumerable: true,
    get: function() {
        return installModule;
    }
});
var _fs = /*#__PURE__*/ _interop_require_default(require("fs"));
var _fsremovecompat = require("fs-remove-compat");
var _mkdirpclassic = /*#__PURE__*/ _interop_require_default(require("mkdirp-classic"));
var _path = /*#__PURE__*/ _interop_require_default(require("path"));
var _queuecb = /*#__PURE__*/ _interop_require_default(require("queue-cb"));
var _tempsuffix = /*#__PURE__*/ _interop_require_default(require("temp-suffix"));
var _constantsts = require("../constants.js");
var _cachets = /*#__PURE__*/ _interop_require_default(require("../lib/cache.js"));
var _parseInstallStringts = /*#__PURE__*/ _interop_require_default(require("../lib/parseInstallString.js"));
function _array_like_to_array(arr, len) {
    if (len == null || len > arr.length) len = arr.length;
    for(var i = 0, arr2 = new Array(len); i < len; i++)arr2[i] = arr[i];
    return arr2;
}
function _array_without_holes(arr) {
    if (Array.isArray(arr)) return _array_like_to_array(arr);
}
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _iterable_to_array(iter) {
    if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter);
}
function _non_iterable_spread() {
    throw new TypeError("Invalid attempt to spread non-iterable instance.\\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _to_consumable_array(arr) {
    return _array_without_holes(arr) || _iterable_to_array(arr) || _unsupported_iterable_to_array(arr) || _non_iterable_spread();
}
function _unsupported_iterable_to_array(o, minLen) {
    if (!o) return;
    if (typeof o === "string") return _array_like_to_array(o, minLen);
    var n = Object.prototype.toString.call(o).slice(8, -1);
    if (n === "Object" && o.constructor) n = o.constructor.name;
    if (n === "Map" || n === "Set") return Array.from(n);
    if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _array_like_to_array(o, minLen);
}
var isWindows = process.platform === 'win32' || /^(msys|cygwin)$/.test(process.env.OSTYPE);
var symlinkType = isWindows ? 'junction' : 'dir';
function installModule(installString, nodeModulesPath, options, callback) {
    var _path1;
    var cachePath = options.cachePath || _constantsts.DEFAULT_CACHE_PATH;
    var name = (0, _parseInstallStringts.default)(installString).name;
    var dest = (_path1 = _path.default).join.apply(_path1, [
        nodeModulesPath
    ].concat(_to_consumable_array(name.split('/'))));
    _fs.default.stat(dest, function(err) {
        if (!err) return callback(null, dest); // already installed
        (0, _cachets.default)(installString, cachePath, function(err, cachedAt) {
            if (err) {
                console.log("Could not install: ".concat(installString, ". Message: ").concat(err.message));
                return callback(err);
            }
            // Use temp symlink + atomic rename to avoid cross-process race conditions
            var tempDest = (0, _tempsuffix.default)(dest);
            var queue = new _queuecb.default(1);
            queue.defer(_mkdirpclassic.default.bind(null, _path.default.dirname(dest)));
            queue.defer(_fs.default.symlink.bind(null, cachedAt, tempDest, symlinkType));
            queue.defer(function(cb) {
                _fs.default.rename(tempDest, dest, function(err) {
                    // If rename fails because dest exists, another process won - that's ok
                    if (err && [
                        'EEXIST',
                        'ENOTEMPTY',
                        'EPERM'
                    ].indexOf(err.code) >= 0) {
                        (0, _fsremovecompat.safeRm)(tempDest, function() {
                            return cb();
                        });
                        return;
                    }
                    cb(err);
                });
            });
            queue.await(function(err) {
                if (err) {
                    (0, _fsremovecompat.safeRm)(tempDest, function() {
                        return callback(err);
                    });
                    return;
                }
                callback(null, dest);
            });
        });
    });
}
/* CJS INTEROP */ if (exports.__esModule && exports.default) { try { Object.defineProperty(exports.default, '__esModule', { value: true }); for (var key in exports) { exports.default[key] = exports[key]; } } catch (_) {}; module.exports = exports.default; }